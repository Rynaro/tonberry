<!--
  SPECTRA owns this artifact's schema — see SPECTRA spec-profile.v1.json.
  This file is the SPECTRA-owned slot for the full-tier change; ESL only
  requires that spec.{md,yaml} be present for tier=full.
-->
# full-new-subsystem — full spec (SPECTRA-owned slot)

A worked illustration only. The real body is SPECTRA's `spec.md` output;
acceptance ids AC-1 / AC-2 are referenced from `change.json.acceptance_checks`.
